import { Button } from "react-bootstrap";
import styled from "styled-components";

const MyBootstrapBtn = styled(Button)`
  width: fit-content;
  padding: 0 !important;
  background: transparent !important;
  border: 0 !important;
  position: absolute;
  bottom: 1.05rem;
  right: 1.62rem;
`;

export default MyBootstrapBtn;
